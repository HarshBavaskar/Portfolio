// Paste the Apps Script web-app URL here once it's deployed
// (see scripts/early-access.gs). While it's empty, sign-ups open a
// pre-filled email instead, so nothing is ever lost.
export const SIGNUP_ENDPOINT = import.meta.env.VITE_SIGNUP_ENDPOINT || '';
export const OWNER_EMAIL = 'hbavaskar6@gmail.com';
